# Implementation status

## M0

状态：部分完成（领域布局已建立；真实 DSH 版本与 API 未核验）。

已完成：TypeScript strict/ESM 项目骨架、任务合同运行时校验、状态转移、完成门禁、路径边界和模型权限边界；未完成：DSH bundle/profile、真实工具注册、Codex/独立审核器适配。

## M1–M3

状态：未完成。任务持久化、worktree、可信验证计划、执行器、审核器、报告、native-fixture 和 live 测试仍需实现。

## 验证记录

尚未安装依赖，`npm test`/`npm run build` 尚未执行。任何未运行项目均不得视为 passed。
