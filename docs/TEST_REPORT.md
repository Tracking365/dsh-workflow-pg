# Test report — 2026-09-23

## Actual environment

Darwin 23.6.0 x86_64; Node v24.14.1; npm 11.11.0; Git 2.53.0. The committed lockfile pins
DSH **0.1.6-alpha.2**, DSH Tools **0.1.6-alpha.2**, DSH Agent/AgentLoop/LLM/Session core
test dependencies **0.1.6-alpha.2**, optional DSH Subagent and Codex Provider
**0.1.6-alpha.2**, Cordis **4.0.3**, TypeScript **5.7.2** and @types/node **22.10.2**. The
repeatable checks below invoked no real model or credential. A separately authorized one-shot
temporary fixture used the current Codex login once; it is not part of the automated count.

## Executed

| Command/layer | Result | What it proves |
|---|---|---|
| `npm ci --ignore-scripts` | passed | Exact committed dependencies reconstructed before the final type check and test run. |
| `npm run check` | passed | Strict TypeScript check with the above locked compiler/types. |
| `npm test` | passed: 100 tests, 0 failures/skips | Original/domain/SQLite/Git-process fixtures, an explicit content-locked native `pagination-v1` lifecycle, durable restart/recovery facts and fresh-clone tests, Git-base frozen-context policy/hash/corruption tests, real Cordis/ToolRuntime and DSH AgentLoop fixtures, candidate-bound DSH session, provider subprocess-seam/cancellation fixtures, strict provider/reviewer configuration guards, managed App Server boundary lifecycle, host-confinement lifecycle, plus synthetic direct App Server JSONL, bounded local approval and recovery brokers/control planes, one-pending-recovery protection, and injected HTTP tests. |
| `npm run demo` | passed | Real failing baseline -> fixed candidate -> real TAP pass -> fixture review -> patch/report, awaiting human acceptance. The task ID and temporary paths are non-portable. |
| `npm run test:pack` | passed | Tarball includes runtime/native/patch/skill/docs, imports without source tree; definition contract checks. |
| `npm run test:seatbelt-host` | passed: 3 tests | Real macOS `sandbox-exec` processes in disposable directories allowed a candidate write, denied a control write and a configured synthetic credential-root read, and denied both loopback TCP and a local Unix socket. The gate also ran a fake package-shaped `@openai/codex` wrapper through the exact App Server boundary, proved an ambient parent variable is absent, the host home cannot be enumerated, and fresh private state is removed only after exit proof. It also ran a Node TAP check through the reusable `runCommand()` boundary. No business repository, real credential, Codex binary, model, or external network was used. |
| Native DSH ToolRuntime fixture | passed | Real `@deepseek-ai/cordis` Context and `@deepseek-ai/dsh-tools` ToolRuntime registered, dispatched, rejected invalid schema input, unloaded and reloaded all DevKit tools. A separately double-opted-in `pagination-v1` policy ran create → reproduce → deterministic write → verify → report → cancel/resume boundary without starting a model. |
| Offline DSH AgentLoop fixture | passed | A local finite `LlmAdapter` drove real `LlmRuntime`/`AgentLoop`/session/prompt/tool services. It selected `devkit_doctor`, persisted its rendered JSON as `tool/result`, fed that result into the next request, and propagated `{ kind: "user" }` cancellation while preserving already streamed text. The adapter has no endpoint, credential or network implementation. |
| DSH Codex registry bridge and candidate-session fixtures | passed | Real `SubagentRuntime` accepts the bounded/redacted bridge request, observes the exact parent/signal, and exercises missing-provider, failed result, unconfirmed teardown, and parent/candidate-workspace mismatch fail-closed paths. A full real AgentLoop fixture then creates a short-lived canonical candidate parent with durable source-parent lineage, reaches a fixture provider, and proves the parent session is released. Separate failure fixtures prove that a parent-create failure starts no child and an unconfirmed parent teardown retains the writer. The real official provider is separately invoked against a throwing subprocess seam; it receives the canonical candidate cwd once and no process is executed. A separate in-memory JSON-RPC peer holds a published turn open; cancellation produces the official provider interrupt, termination/wait-for-exit, `CODEX_SUBAGENT_ABORTED`, candidate parent/session release, and the exact `approve-for-me` `thread/start` fields including `sandbox: "workspace-write"`. That peer is also exercised through a preflighted Seatbelt App Server boundary; private boundary state survives until `waitForExit()` proof. |
| Official Codex provider and reviewer configuration guards | passed | Real `@deepseek-ai/dsh-subagent-codex` registers into a real `SubagentRuntime`; native `doctor` observes its declared mode, refuses full-access/unreadable/nonempty-env providers, and reports `approve-for-me` plus `env: {}` only as provider-protocol eligible. It explicitly keeps writer launch false because the locked provider has no authenticated approval bridge and DevKit has no credential broker. A disabled policy may mount exactly that provider in a DevKit-owned root subprocess scope; external provider mounts cannot construct the executor. DevKit also captures the provider's otherwise-discarded registry disposer, and the fixture proves unload/reload does not leave a stale `codex` provider. It validates a separate DeepSeek HTTPS/model/`DSH_DEVKIT_*` credential reference without reading the value or calling an endpoint. Deliberately throwing subprocess seams recorded zero starts, so no App Server/model process ran. |
| Direct App Server + local approval/recovery control planes | passed: synthetic/local only | A fake stdio server verified stable-only initialize/thread/turn sequencing, ephemeral-thread enforcement, candidate-only `workspaceWrite` with restricted reads and command network disabled, current thread/turn/item binding for item and terminal notifications, cached documented command facts, automatic decline for cross-thread/network/permission or exec-policy-expansion requests, startup/turn cancellation with `turn/interrupt`, exit proof, and a failed-release path. A separate launch fixture verified the exact Seatbelt-wrapped wrapper argv/env shape and the post-preflight cancellation race. The local approval queue/page covers one-time binding/expiry/abort/close plus queue/audit caps, secret login, `HttpOnly` SameSite session, CSRF, origin checks, escaping, bounded sessions and shutdown. The separate recovery page covers the same local session boundary plus an old-writer-stopped proof, exact retained-run binding, fresh-clone-only requeue, shutdown retention, and direct duplicate-recovery rejection. Native ToolRuntime starts either page only from explicit disabled host policy and a synthetic `DSH_DEVKIT_*` secret, rejects missing secrets, tears listeners down, and exposes no recovery task tool; no Codex binary, current login, endpoint, or model was used. |
| `npm run test:codex-provider-profile` | passed | Packages the installed exact official provider, adds it to a disposable headless DSH profile, confirms its `subagent-codex` bundle row, and boots the profile twice. No task or `SubagentRuntime.start()` call is made. |
| `npm run test:launcher` | passed | Official DSH creates a disposable headless profile, installs the current tarball, discovers its bundle via `--dump-config`, then boots it twice with `--help`. A temporary disabled-policy SQLite file proves native `apply()` mounted; no task/model request is supplied. |
| Authorized one-shot live Codex fixture (manual, non-CI) | passed: one invocation | A real official App Server ran through `DshCandidateWorkspaceCodexExecutor` with the current login, `permissionMode: "never"`, and `env: {}`. The controller and candidate were separate temporary directories. The observed subprocess started exactly once with the canonical candidate cwd; it completed, left `README.md` unchanged, created no unexpected top-level files, and wrote the exact allowed proof file. No full-access mode, remote, push, merge, or deployment was configured. This does not prove OS containment, network-egress control, credential non-disclosure, or actual-App-Server cancellation. |
| Direct App Server / live safety boundaries | partial/blocked | The synthetic direct client and local approval UI close protocol/presentation gaps; the page is now native-policy-wired but remains disconnected from the executor and cannot obtain credentials or network access. Official documentation supports a private App-Server-managed ChatGPT OAuth lifecycle, but no OAuth flow was started or tested and existing login state was not inspected. One narrow, explicit current-login fixture still only proves the legacy provider's startup, cwd handoff, teardown, and a constrained write. A real Seatbelt host fixture wraps a fake App-Server-shaped child and proves TCP/Unix-socket denial, host-home non-enumerability and cleanup-after-exit proof. Actual-App-Server cancellation, direct-client native execution, isolated OAuth/outbound transport, a complete read whitelist or independently killable sandbox, independent reviewer behavior, and a deployed live task remain unverified. |

The demo produced `awaiting_human`, `readyForAcceptance:true`, and
`realModelsUsed:false`. Generated task IDs and fixture paths are local runtime artifacts, not
portable references in this repository. Run demo to obtain fresh paths. Unit tests execute
fixture authors/reviewers as deterministic host code, not as real AI models. The HTTP adapter
test uses an injected transport.

## Coverage against the original acceptance IDs

`partial` below means only the stated subcase was exercised, not full acceptance.

| IDs | Status | Evidence / limit |
|---|---|---|
| A01 | partial | Exact DSH/Cordis/agent-loop/subagent npm packages, types, source paths, ToolRuntime/provider registration fixtures, an offline real AgentLoop session, candidate-bound parent composition and official provider cwd-seam fixture, plus official launcher profile boot checked. One separate authorized current-login App Server fixture completed with the canonical candidate cwd and exact scoped write; no general credential or remote-model boundary is proven. |
| A02 | partial | Tarball/import and official profile install/config discovery passed; no production profile or live task. |
| A03 | partial | Real published ToolRuntime dispatches doctor/create/status/run/report/cancel/resume; its content-locked `pagination-v1` fixture reaches final acceptance without a model, and doctor detects a registered official Codex provider. An offline real AgentLoop session selects `devkit_doctor`; a separate real AgentLoop fixture creates the candidate-bound parent passed to the provider seam. One manual live Provider process completed only in a temporary isolated fixture, not through the native task tool. |
| A04 | partial | ToolRuntime unload/reload, owned cooperative shutdown, bridge disposal semantics, candidate-parent handle disposal and unconfirmed-parent retention, offline AgentLoop cancellation, official-provider in-memory wire cancellation, and two launcher boots passed; actual-App-Server cancellation remains unverified. |
| A05 | partial | Strict nested input plus actual ToolRuntime invalid-schema rejection; the offline AgentLoop proves native schema presentation and rendered output replay, while a live launcher/provider presentation path remains unverified. |
| B01–B04 | passed (unit/fixture) | Create, same/different idempotency keys, unsupported kinds. |
| B05–B06 | partial | Missing/same-family adapters, unknown authority fields, bounded/redacted Codex task construction, candidate-parent cwd handoff and parent/candidate mismatch are blocked or fixture-tested. One real provider invocation performed the exact allowed proof write in a temporary fixture; full live/prompt-injection testing remains. |
| C01–C03 | passed (fixture) | Dirty source preserved; path variants/symlinks rejected; persistent leases and concurrent run rejection. |
| C04–C08 | partial/blocked | Snapshot mutation, strict live block, candidate-workspace parent composition and official-provider cwd seam tested; one real provider process observed the candidate cwd and exact allowed proof write. Native code also refuses full-access, unreadable/unknown, or nonempty-env providers and tests the provider-declared `workspace-write` wire field. A managed App Server launch boundary has host proof with a fake package-shaped child: candidate-only writes, configured protected-read and host-home directory-data denial, TCP/Unix-socket denial, and exit-proof cleanup. It does not prove an actual App Server's inner sandbox, credential secrecy, a complete host read boundary, or model communication. |
| C09 | partial | Shell-free argv/env test on macOS; Windows not tested. |
| D01–D06 | passed (fixture) | Frozen regression failure/pass, no reproduction, missing/zero/skipped tests, tampering, stale evidence. |
| D07–D09 | passed (fixture) | Verifier mutation, binary/untracked/executable snapshot, incomplete acceptance coverage. |
| E01–E06 | partial | Confirmed/unconfirmed P1, low-priority backlog, malformed/stale/failed HTTP reviews and shared retry budget tested; production evidence adjudication/optional-review behavior absent. |
| E07–E09 | partial | Deterministic fingerprints, same-family block and stale snapshot checks; semantic dedup and full live identity checks remain. |
| F01–F02 | partial | macOS process/cooperative cancellation, native fixture terminal cancellation, offline AgentLoop stream cancellation/preserved text, bridge and candidate-parent disposal proof, official-provider in-memory published-turn interrupt/termination/wait-for-exit, candidate-parent create failure, and unproven stop retaining the lease are tested; both Seatbelt command and App Server boundaries retain private state until settlement/exit proof. Escaped hostile descendants and real-App-Server cancellation still require provider/live evidence. |
| F03–F05 | partial | Reopen atomically marks unfinished writers `interrupted`, retains the lease and records restart evidence. A host-only fixture authorization is bound to task/run/lease/snapshot facts, proves an old writer stopped, then rechecks for TOCTOU before preserving the interrupted candidate and queueing a fresh base clone. A changed candidate during authorization retains its lease. An explicit disabled native policy can start a separately authenticated local recovery page; it requires an operator assertion, binds one pending recovery per task, exposes no recovery tool, and preserves the lease on shutdown. It is not multi-user identity or live writer/process proof. |
| F06–F07 | passed (unit) | Unsupported/corrupt stores preserved; failing event trigger rolls back task state. |
| G01–G02 | passed (fixture) | Distinguishes final acceptance from unresolved human judgement. |
| G03–G04 | partial | No model approval or recovery tool, stale host acceptance rejected; local approval/recovery pages provide bounded authenticated single-host sessions and expiring decisions, but multi-user authorization remains absent. |
| G05 | passed (fixture) | Patch delivered; dirty source unchanged; no push/merge/deploy. |
| G06 | deferred | No production local commit feature. |
| G07–G08 | partial | Bounds/redaction/env and an honest macOS-only matrix; regex is not complete secret detection. |
| H01–H03 | partial/fail-closed | No implicit model invocation/fallback; a registered provider still cannot bypass disabled mode or the workspace-binding guard. Full-access, unreadable/unknown, or nonempty-env providers are blocked; native policy can authenticate a local approval presentation but cannot construct a direct-client executor until isolated managed-OAuth/outbound-transport boundaries exist. The tested `approve-for-me` wire maps to an explicit provider-declared workspace-write request but not an OS boundary. One separately authorized fixture invoked the real provider with `permissionMode: "never"` and a temporary cwd, but does not establish a general live safety boundary. |

100/100 does not mean the 56 acceptance IDs all pass; the synthetic direct-client/control-plane tests, one manual provider invocation and the separate host Seatbelt gate are not production evidence. M0–M3 are explicitly incomplete.
